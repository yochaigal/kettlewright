1. Copy contents of repository to new folder (except for .git). 
2. Move .env.template to .env.
3. Fill out .env variables.
4. Build docker image with 'docker build -t kettlewright .'
5. Run 'docker run -it --env-file .env kettlewright /bin/sh'
6. Run 'flask db upgrade' and then 'exit'
7. Launch with 'docker run --user $(id -u):$(id -g) -v $(pwd):/app --env-file .env -p 5000:5000 kettlewright'
8. Access the application at http://127.0.0.1:5000
